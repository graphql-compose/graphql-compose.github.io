version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.10

    steps:
      - checkout
      - run:
          name: Deploying to GitHub Pages
          command: |
            git config --global user.email "nodkz@users.noreply.github.com"
            git config --global user.name "nodkz"
            echo "machine github.com login nodkz password $GITHUB_TOKEN" > ~/.netrc
            cd website && yarn install && GIT_USER=nodkz yarn run publish-gh-pages

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build:
